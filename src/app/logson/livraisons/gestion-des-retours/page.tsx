import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function GestionRetoursPage() {
  return (
    <Card className="w-full">
      <CardHeader>
        <CardTitle className="text-2xl">Gestion des Retours</CardTitle>
        <CardDescription>Gérez les retours de produits.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la section de la gestion des retours est en cours de construction.</p>
      </CardContent>
    </Card>
  );
}
