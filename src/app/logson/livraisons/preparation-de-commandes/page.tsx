import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function PreparationCommandesPage() {
  return (
    <Card className="w-full">
      <CardHeader>
        <CardTitle className="text-2xl">Préparation de Commandes</CardTitle>
        <CardDescription>Gérez la préparation des commandes clients.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la section de la préparation de commandes est en cours de construction.</p>
      </CardContent>
    </Card>
  );
}
