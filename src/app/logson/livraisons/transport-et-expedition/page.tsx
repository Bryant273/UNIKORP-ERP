import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function TransportExpeditionPage() {
  return (
    <Card className="w-full">
      <CardHeader>
        <CardTitle className="text-2xl">Transport et Expédition</CardTitle>
        <CardDescription>Gérez les expéditions de vos commandes.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la section du transport et de l'expédition est en cours de construction.</p>
      </CardContent>
    </Card>
  );
}
