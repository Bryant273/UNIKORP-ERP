import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function EtatsComptablesJournauxPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-2xl">Journaux (États Comptables)</CardTitle>
        <CardDescription>Consultation des états des journaux.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la section des journaux (états comptables) est en cours de construction.</p>
      </CardContent>
    </Card>
  );
}
