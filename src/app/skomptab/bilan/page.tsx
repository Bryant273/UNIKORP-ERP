import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function BilanPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-2xl">Bilan</CardTitle>
        <CardDescription>Génération et consultation du bilan.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la section du bilan est en cours de construction.</p>
      </CardContent>
    </Card>
  );
}
