import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function CreationJournauxPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-2xl">Journaux</CardTitle>
        <CardDescription>Création et gestion des journaux comptables.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la section de création des journaux est en cours de construction.</p>
      </CardContent>
    </Card>
  );
}
