import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function CalendrierFiscalPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-2xl">Calendrier Fiscal</CardTitle>
        <CardDescription>Consultation du calendrier fiscal.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la section du calendrier fiscal est en cours de construction.</p>
      </CardContent>
    </Card>
  );
}
