import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function SaisieComptablePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-2xl">Saisie Comptable</CardTitle>
        <CardDescription>Interface de saisie des écritures comptables.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la section de saisie comptable est en cours de construction.</p>
      </CardContent>
    </Card>
  );
}
